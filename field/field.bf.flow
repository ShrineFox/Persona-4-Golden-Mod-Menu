import( "../build/input/extracted/field/script/field.bf" );
import( "../ModMenu.flow" );

//ALTERNATE MOD COMBINATIONS (enable only one)
//import ( "../Default.flow" ); //Mod Menu only (no QuickTravel)
//import ( "../QuickTravel.flow" ); //Choose between Mod Menu or QuickTravel
import ( "../MobileCalendar.flow" ); //Same as above but includes "View calendar" option
//import ( "../MobileCalendarNoModMenu.flow" ); //Same as above but does NOT include "Mod menu" option

//EXTENSIONS
import ( "../QuickTravelPlus.flow" ); //Extended QuickTravel locations for town/school